package it.polimi.ingsw.AntoniniCastiglia;

import static org.junit.Assert.*;

import java.io.IOException;
import java.util.ArrayList;

import it.polimi.ingsw.AntoniniCastiglia.maps.Sector;
import it.polimi.ingsw.AntoniniCastiglia.maps.Table;

import org.junit.Test;

public class TableTest {
/*
	@Test
	public void testAdjacent() throws IOException {
		Table t = new Table();
		boolean check = true;
		ArrayList<Sector> expected = new ArrayList<Sector>();
		ArrayList<Sector> produced = new ArrayList<Sector>();

		expected.add(new Sector("B01"));
		expected.add(new Sector("B02"));
		expected.add(new Sector("A03"));

		produced = t.adjacent(new Sector("A02"), 1);

		for (int i = 0; i < produced.size(); i++) {
			if (!(expected.contains(produced.get(i)))) {
				check = false;
			}
		}
		assertTrue(check);
	}
*/
}
